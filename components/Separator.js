import { View } from "react-native";
import React from "react";

const Separator = () => {
    return (
      <View
        style={{
          borderBottomColor: 'white',
          borderBottomWidth: 1,
          marginVertical: 10,
          width: '100%',
        }}
      />
    );
  };

  export default Separator;
  